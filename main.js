const {app,BrowserWindow}=require('electron');const fs=require('fs');function create(){const w=new BrowserWindow({width:1600,height:950,minWidth:1100,minHeight:700,backgroundColor:'#17191d',webPreferences:{contextIsolation:true,sandbox:true}});w.loadFile('index.html').then(()=>w.webContents.executeJavaScript(fs.readFileSync(__dirname+'/live.js','utf8')))}app.whenReady().then(()=>{create();app.on('activate',()=>BrowserWindow.getAllWindows().length===0&&create())});app.on('window-all-closed',()=>{if(process.platform!=='darwin')app.quit()});
